<% provide(:title, "New Event") %>

<div class="p-events">


  <section class='o-pageContent p-events__content'>
    <div class='o-pageContent__section p-events-content__editing container'>
      <h2 class='boxed'>New Event</h2>

      <%= render 'form' %>

      <%= link_to(content_tag(:i, ' Back', class: 'fa fa-hand-o-left'), :back) %>
    </div>
  </div>
